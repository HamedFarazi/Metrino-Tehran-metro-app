# تهران مترو - سیستم تصاویر ایستگاه‌ها از ویکی‌مدیا کامنز

## 📸 معرفی

این سیستم تصاویر ایستگاه‌های مترو تهران را از ویکی‌مدیا کامنز با رعایت کامل قوانین مجوز و حق نشر دریافت می‌کند.

## 🎯 ویژگی‌ها

- ✅ دریافت خودکار تصاویر از ویکی‌مدیا کامنز
- ✅ اعتبارسنجی مجوزهای Creative Commons
- ✅ دانلود و بهینه‌سازی تصاویر (تبدیل به WebP)
- ✅ نمایش تصاویر در رابط کاربری برنامه
- ✅ صفحه اعتبار تصاویر با اطلاعات کامل
- ✅ کار در حالت آفلاین

## 🚀 نصب و راه‌اندازی

### 1. پیش‌نیازها

```bash
# اطمینان از نصب sharp (برای پردازش تصاویر)
npm install --save-dev sharp
```

### 2. دستورات موجود

```bash
# تست اتصال به API ویکی‌مدیا
npm run images:test-api

# تولید نگاشت نام‌های ایستگاه‌ها
tsx scripts/station-name-mapper.ts

# دریافت تصاویر ایستگاه‌ها
npm run images:fetch
```

## 🔧 نحوه کار

### فاز 1: تولید نگاشت نام‌ها

اسکریپت `station-name-mapper.ts` نام‌های انگلیسی و فارسی ایستگاه‌ها را به فرمت مناسب ویکی‌مدیا تبدیل می‌کند.

### فاز 2: جستجوی ویکی‌مدیا

اسکریپت اصلی `fetch-station-images.ts`:
1. داده‌های ایستگاه‌ها را می‌خواند
2. برای هر ایستگاه دسته‌بندی‌های ویکی‌مدیا را جستجو می‌کند
3. تصاویر موجود را بررسی می‌کند
4. مجوزها را اعتبارسنجی می‌کند
5. تصاویر مناسب را دانلود و بهینه‌سازی می‌کند

### فاز 3: اعتبارسنجی مجوز

سیستم فقط مجوزهای زیر را می‌پذیرد:
- CC BY-SA 4.0
- CC BY-SA 3.0
- CC BY 4.0
- CC BY 3.0
- CC0
- Public Domain
- GFDL

### فاز 4: بهینه‌سازی تصاویر

تصاویر با مشخصات زیر بهینه می‌شوند:
- فرمت: WebP
- حداکثر عرض: 1600px
- کیفیت: 85%
- ذخیره در: `public/stations/`

## 📁 ساختار فایل‌ها

```
scripts/
├── fetch-station-images.ts     # اسکریپت اصلی
├── test-wikimedia-api.ts      # تست API
└── station-name-mapper.ts     # تولید نگاشت نام‌ها

src/
├── types/station-images.ts    # تایپ‌های TypeScript
├── services/station-images.service.ts  # سرویس تصاویر
└── features/station/StationImage.tsx  # کامپوننت تصویر

public/
└── stations/                  # تصاویر دانلود شده
```

## 🖼️ نمایش در UI

### کامپوننت StationImage

کامپوننت `StationImage` تصاویر ایستگاه‌ها را با موارد زیر نمایش می‌دهد:
- تصویر اصلی یا طرح جایگزین
- نشانگر خطوط مترو
- اطلاعات اعتبار تصویر
- حالت پاسخگو

### صفحه اعتبار تصاویر

صفحه `ImageCreditsPage` شامل:
- لیست تمام تصاویر
- فیلتر بر اساس مجوز و عکاس
- آمار کلی
- لینک به صفحات منبع

## ⚠️ ملاحظات

### محدودیت‌های نرخ درخواست

ویکی‌مدیا API محدودیت نرخ درخواست دارد:
- تأخیر 1 ثانیه بین درخواست‌ها
- حداکثر 50 تصویر در هر ایستگاه
- زمان‌بندی مناسب برای 150+ ایستگاه

### حقوق نشر

- **هرگز** از Google Maps یا Google Images استفاده نکنید
- **هرگز** تصاویر بدون مجوز واضح دانلود نکنید
- **همیشه** اطلاعات اعتبار را حفظ کنید
- **همیشه** به قوانین robots.txt احترام بگذارید

### ذخیره‌سازی محلی

تصاویر به صورت محلی ذخیره می‌شوند تا:
- در حالت آفلاین کار کنند
- سرعت بارگذاری افزایش یابد
- وابستگی به اینترنت حذف شود

## 📊 گزارش‌گیری

پس از اجرای اسکریپت، گزارش‌های زیر تولید می‌شوند:
- `scripts/image-fetch-log.json`: لاگ کامل فرآیند
- `scripts/image-fetch-summary.json`: خلاصه آمار
- `src/data/station-images.json`: داده‌های تصاویر
- `src/data/station-images.ts`: فایل TypeScript

## 🔄 اجرای مجدد

اسکریپت به گونه‌ای طراحی شده که:
- تصاویر موجود را دوباره دانلود نمی‌کند
- پیشرفت را ذخیره می‌کند
- در صورت خطا از نقطه توقف ادامه می‌دهد

## 🐛 عیب‌یابی

### مشکل: API ویکی‌مدیا پاسخ نمی‌دهد
```bash
# تست اتصال
npm run images:test-api

# بررسی User-Agent
# اطمینان از تنظیم User-Agent مناسب
```

### مشکل: تصاویر دانلود نمی‌شوند
- بررسی اتصال اینترنت
- بررسی محدودیت‌های نرخ درخواست
- بررسی مجوز تصاویر

### مشکل: TypeScript خطا می‌دهد
```bash
# کامپایل TypeScript
npm run build

# بررسی خطاها
```

## 📞 پشتیبانی

برای گزارش مشکلات یا پیشنهادات:
1. بررسی مستندات
2. اجرای تست‌ها
3. گزارش issue در GitHub

## 📝 مجوز

این سیستم تحت مجوزهای Creative Commons تصاویر ویکی‌مدیا عمل می‌کند. تمام تصاویر باید با رعایت قوانین مجوز مربوطه استفاده شوند.

---

**توجه**: این سیستم فقط برای استفاده غیرتجاری و آموزشی طراحی شده است. استفاده تجاری نیاز به بررسی مجوزهای هر تصویر به صورت جداگانه دارد.